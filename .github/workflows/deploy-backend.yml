name: Deploy Backend

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          sparse-checkout: backend

      - name: Create backend env file
        working-directory: ./backend
        env:
          LIFELOGGER_DATABASE_URL: ${{ secrets.LIFELOGGER_DATABASE_URL }}
          DATABASE_URL_SECRET: ${{ secrets.DATABASE_URL }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          DATABASE_URL="${LIFELOGGER_DATABASE_URL:-$DATABASE_URL_SECRET}"

          if [ -z "$DATABASE_URL" ]; then
            echo "Database URL secret is required."
            echo "Add LIFELOGGER_DATABASE_URL, or DATABASE_URL, as a repository secret."
            exit 1
          fi

          {
            printf 'DATABASE_URL=%s\n' "$DATABASE_URL"
            printf 'HF_TOKEN=%s\n' "$HF_TOKEN"
            printf 'SPEAKER_MATCH_THRESHOLD=0.82\n'
            printf 'MAX_WORKERS=2\n'
            printf 'OMP_NUM_THREADS=2\n'
            printf 'MKL_NUM_THREADS=2\n'
            printf 'OPENBLAS_NUM_THREADS=2\n'
          } > .env

          chmod 600 .env
          echo "Created backend/.env for deployment."

      - name: Deploy backend with docker compose
        working-directory: ./backend
        run: |
          docker compose down
          docker compose build
          docker compose up -d

      - name: Wait for health check
        run: |
          echo "Waiting for API to be ready..."
          for i in $(seq 1 30); do
            if curl -sf http://localhost:10000/health > /dev/null 2>&1; then
              echo "API is healthy!"
              exit 0
            fi
            sleep 2
          done
          echo "Health check failed after 60 seconds"
          docker compose -f backend/docker-compose.yml logs api
          exit 1
